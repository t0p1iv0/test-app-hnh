package creatures;

public class Monster extends Creature {

    public Monster(int attack, int defence, int maxHP, int minDamage, int maxDamage, String name) {
        super(attack, defence, maxHP, minDamage, maxDamage, name);
    }
}
